package com.phonegap.geoquestweb;


import static org.junit.Assert.*;

import org.junit.Test;

public class TestBarcodeScanner {

	@Test
	public void testScan(){
		/*run a scan and check if there are no errors
		 *check if the scanner returns some valid data string or binary representation
		 * */
		assertTrue(true);
	}
	
	@Test
	public void testEncode(){
		/*encode some data with no errors
		 *check if the encoder gives a valid result 
		 *(an image, a representation from which a QR code can be restored or anything similar) 
		 * */
		assertTrue(true);
	}
	
	@Test	
	public void testEncodeAndScan(){
		/*create an input for the encoder
		 *run encoder
		 *scan the result of the encoder
		 *check if the return value from the scanner equals the input value for the encoder
		 * */		
		
		assertTrue(true);
		
		
	}
	
	
}
